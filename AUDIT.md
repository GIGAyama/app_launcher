# GIGA Standard v5 監査：GIGA portal

- **型**：D型（Chrome拡張 Manifest V3）
- **監査日**：2026-08-05
- **監査した版**：`8a90865`（改修前）→ `claude/rollout-fn257q`（改修後）
- **測り方**：`tools/measure.mjs`（実ブラウザで全画面を走査）と
  `tools/verify-extension.mjs`（実物の Chrome へ拡張機能として読み込ませる）

> **「未計測」は ✅ ではない。** 測っていないものは下の「測っていないもの」に書いた。
> 実測値はすべて Chromium 141（Playwright 同梱）での測定値である。

---

## いちばん大きかった問題

**学校のフィルタリング下で、既定の9個のアイコンが1枚も表示されていなかった。**

改修前の commit を実際に Chrome へ拡張機能として読み込ませて測った結果：

| | 改修前 | 改修後 |
|---|---:|---:|
| 絵が出ないアイコン | **9 / 9 枚** | **0 / 9 枚** |
| CSP違反 | **18件** | **0件** |

原因は2つ重なっていた。

1. 既定の9個のアイコンは `ssl.gstatic.com` から取っている。
   学校のネットワークがここを塞いでいると1枚も出ない。
2. 代わりの絵に切り替える仕組みが `onerror=` 属性で書かれていた。
   MV3 の拡張機能ページには既定で `script-src 'self'` が効いているため、
   **インラインのイベント属性は一切実行されない。**
   つまり「代わりの絵」は最初から一度も出たことがなかった。

**2 は読むだけでは気づけない。** 属性は正しく書かれており、構文も正しい。
実際に読み込ませて初めて分かる種類の不具合である。

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama |
| A2 | .gitignore | ✅ | node_modules / dist / .env / .DS_Store |
| A3 | dependabot.yml | ✅ | npm・github-actions を monthly、メジャー更新は人間が判断 |
| A4 | README / MANUAL / AUDIT | ✅ | 3点とも用意 |
| A5 | CI | ✅ | `pull_request` と `push:[main]` の両方で動く |
| A6 | 秘密情報のコミット | ✅ | `git ls-files` に `.env` / `.clasp.json` なし。APIキー・IDの直書きなし |

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（**入れたうえで動作確認済み**） | ✅ | `manifest.json` に明記。実物の Chrome で CSP違反 0件・インライン script が止まることを確認 |
| B2 | 秘密情報・IDの直書きなし | ✅ | なし |
| B3 | 権限が最小 | ⚠️ | `tabs` は「開いているタブを探す」機能に必須のため残した（→ 人間に確認してほしいこと） |
| B4 | postMessage の宛先が `*` でない | ✅ | 改修前は `'*'`。埋め込み元のオリジンを iframe の URL で受け渡し、そこだけに送る形にした |
| B5 | 受信側が送り主を確かめる | ✅ | 改修前は無検査。`e.origin` と `e.source` の両方を確認 |
| B6 | CDN から取る実行コードが 0 | ✅ | **0 バイト**。`@babel/standalone` / Tailwind CDN / CDN の React いずれも無し。`fonts.googleapis.com` の `@import` は削除 |
| B7 | 外部から読む実行コードへの SRI | — | 対象なし（外部から読む実行コードが 0 のため） |
| B8 | 外から来る文字列の扱い | ✅ | 改修前はアプリ名を `innerHTML` に差し込んでいた。`textContent` で組み立てる形に変更 |
| B9 | 取り込む設定ファイルの検査 | ✅ | 改修前は `url` の有無のみ。`javascript:` を含む設定ファイルを読み込ませると、`window.open` 経由で拡張機能の権限でコードが動く状態だった。http/https だけを通す検査を追加（テストあり） |
| B10 | web_accessible_resources の範囲 | ✅ | `popup.html` のみ・`*.google.com` / `*.google.co.jp` に限定（改修前は css/js も公開） |

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | 保存の失敗を知らせる | ✅ | 改修前は `chrome.storage.sync` の容量超過で保存が黙って失敗し、何も出ずに画面が戻っていた。この端末へ退避したうえで知らせる形に変更 |
| C2 | 読み込み失敗の明示 | ✅ | トーストで通知（改修前から） |
| C3 | localStorage.clear() を使っていない | ✅ | 使用なし。`chrome.storage.*.clear()` も無し |
| C4 | タブ探しの取り違え | ✅ | 改修前は `https://docs.google.com/` の前方一致がスライド・スプレッドシートのタブにも当たり、「ドキュメント」を押すとスライドが前に出ることがあった。より長い宛先を持つアプリに譲る形に変更（テストあり） |
| C5 | Google のヘッダーが遅いとき | ✅ | 改修前は `setTimeout(…, 1500)` の一発勝負で、通信が遅い端末では差し替えが起きなかった。`MutationObserver` で待つ形に変更（15秒で打ち切り） |
| C6 | 中核ロジックのテスト | ✅ | 14件（URL の検査・設定ファイルの取り込み・タブ探し） |

## D. 表示（Part I §2）

すべて実ブラウザで、**ライト／ダークの両方**・**全4画面**・
**ホバーとフォーカスを当てた状態**を走査した実測値。

| # | 項目 | 判定 | 実測（前 → 後） |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | 指定なし → あり |
| D2 | 100dvh を使用 | — | 対象なし（`100vh` の使用が 0 件） |
| D3 | safe-area-inset を適用 | ✅ | 0箇所 → body の左右・footer の下・iframe の右に適用（Chromebook では実効 0px） |
| D4 | clamp() による fluid type | ✅ | 固定px → `--fs-body` / `--fs-name` / `--fs-title` / `--fs-small` |
| D5 | Canvas に devicePixelRatio 補正 | — | 対象なし（Canvas を使っていない） |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **あり（340 > 320）→ なし** |
| D7 | 画像に width/height | ✅ | 指定なし → `width=44 height=44` |
| D8 | **コントラスト 4.5:1 以上** | ✅ | **4件 → 0件** |
| D9 | **タップ領域 44px 以上** | ✅ | **32件 → 0件** |
| D10 | prefers-reduced-motion（`.01ms`） | ✅ | 未対応 → 対応（`0` ではない） |
| D11 | forced-colors 対応 | ✅ | 未対応 → 対応 |
| D12 | 提示モード | — | 対象外（一斉授業で提示する画面ではない） |
| D13 | 印刷CSS | — | 対象外（印刷する画面が無い） |
| D14 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` いずれも無し |

### D8 の内訳（基準未満だったもの）

| 文言 | 色 | 比 | どこ |
|---|---|---:|---|
| 「保存しました」 | 白 on `#81c995` | **1.96** | ダークモードのトースト |
| 「正しい設定ファイルではありません」 | 白 on `#f28b82` | **2.39** | ダークモードのトースト |
| 削除ボタンの `×`（マウスを乗せたとき） | 白 on `#f28b82` | **2.39** | ダークモード |
| （同上・別画面での再掲） | 白 on `#f28b82` | **2.39** | ダークモード |

4件ともダークモードのみ。ダークの強調色は明るいパステルなのに、
文字色が白のまま決め打ちされていたため。
**いちばん読めなかったのは、いちばん困っている人に向けた文だった**
（保存できたのか分からないとき／設定ファイルがうまくいかないとき）。

面と対になる文字色 `--on-accent` を用意し、明るい面には黒側を載せる形にした。
改修後の比は「保存しました」8.21、エラー文 6.71。

**通ってはいたが、ぎりぎりだったもの**（基準未満ではないので上の4件には数えていない）：

| 対象 | 色 | 前 | 後 |
|---|---|---:|---:|
| ボタンの文字・白抜き文字 | `#1a73e8` → `#1967d2` | 4.51 | **5.40** |
| フッター・注釈（11〜12px） | `#6c757d` → `#5f6368` | 4.69 | **6.05** |

Chromebook の液晶はコントラストが弱いため、色相を変えずに1段濃くした。

### D9 の内訳

改修前は **押せるところが1つも 44px に届いていなかった**。

| 対象 | 前 | 後 |
|---|---|---|
| 編集・削除ボタン | 24×24（×18個） | 28×28 ＋ 疑似要素で 44×44 |
| 設定ボタン | 28×28 | 28×28 ＋ 疑似要素で 44×44 |
| 各種ボタン | 34〜36px 高 | `min-height: 44px` |
| 入力欄 | 38px 高 | `min-height: 44px` |
| フッターのリンク「GIGA山」 | **31.9×10** | 疑似要素で 44×44 |

編集と削除のボタンは 16px 空けてある。44px の当たり判定を重ねると
「編集」を狙った指が「削除」に当たることがあり、消えて困るのは児童のほうだから。

## E. PWA（Part I §3）

**この型では対象外。** 拡張機能そのものがインストール対象であり、
`manifest.webmanifest` / `sw.js` / `offline.html` / maskable アイコンは持たない。
（`background.js` は Service Worker だが、これは MV3 の拡張機能の常駐処理であって
オフライン用のキャッシュ制御ではない。キャッシュ操作・`localStorage` 操作はしていない。）

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | アイコンは `alt=""`（名前が隣にあるため）、アイコンのみのボタンに `aria-label`、中の svg に `aria-hidden`。トーストは成功が `status`、エラーが `alert` |
| F2 | モーダルの扱い | — | アプリ独自のモーダルは無し。削除の確認はブラウザの `confirm()`（→ 測っていないもの参照） |
| F3 | キーボードのみで全機能に到達 | ✅ | **改修前は編集・削除に到達できなかった**（`:hover` でしか出ないため）。`:focus-within` を追加。並べ替えも Alt+←/→ でできるようにした |
| F4 | rt の色を決め打ちしていない | — | 対象なし（ふりがなを使っていない）。品質ゲートの検査項目には含めてある |
| F5 | 初回JS 300KB以下 | ✅ | **24.5 KB**（popup.js + lib/） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大 popup.js の 406行 / 18.3 KB |
| F7 | 総アセット 1MB以下 | ✅ | **57.0 KB** |
| F8 | HTML の妥当性 | ✅ | 改修前は `<a>` の中に `<button>` があった（不正・Tab順が壊れる）。兄弟に分離 |

## G. 学習ログ

**対象外。** 学習の記録を取るアプリではなく、`study.v1` は持たない。
個人情報も一切保存しない（保存するのはアプリ名・URL・アイコンURLのみ）。

## H. 性能（§2-6 / §8）

| ファイル | 前 | 後 |
|---|---:|---:|
| `icon.png`（512×512 フルカラー） | 121.5 KB | （原本として `tools/` へ退避。配布物から外した） |
| `icons/icon-16.png` | — | 0.3 KB |
| `icons/icon-48.png` | — | 0.6 KB |
| `icons/icon-128.png` | — | 1.6 KB |
| 代替アイコン SVG 10点 | — | 4.4 KB |
| **配布物のアイコン合計** | **121.5 KB** | **6.9 KB（-94.3%）** |

`manifest.json` は 16/48/128 の3枚とも 512×512 の1枚を指していた。

---

## 品質ゲート

`node scripts/check-project.mjs` … **違反 0件**（15項目）

`node scripts/check-project.mjs --selftest` … **15項目すべてがわざと壊した中身に反応**

**「0件でした」だけでは、基準を満たしているのか検査が何も見ていないのかを
区別できない。** そのため CI では毎回、わざと壊して捕まることを確かめている。

この確認をしたことで、検査そのものの不具合が2件見つかった。

| 見つかった不具合 | 中身 |
|---|---|
| `INLINE_EVENT_HANDLER` の誤検知 | このリポジトリのコードにある注意書き「⚠️ `onerror=` 属性は CSP に阻まれて実行されない」に反応していた。判定前にコメントを落とすようにした |
| `LEGAL_FILES` の誤報 | `.github/**` を検査対象から外していたため、存在する `dependabot.yml` を「無い」と報告していた |

---

## 測っていないもの（**「未計測」は ✅ ではない**）

1. **実機の Chromebook・iPad での表示。**
   測定はすべて Linux 上の Chromium（Playwright 同梱・141）で行った。
   フォントの選ばれ方と行の折り返しは端末で変わるため、実機での確認が要る。
   とくに `--font-ui` の並びのうちどれが当たるかは端末依存である。

2. **ツールバーの吹き出し（本来の popup）としての挙動。**
   検証では `chrome-extension://<id>/popup.html` をタブとして開いている。
   吹き出しには「高さが内容で決まる」「フォーカスが外れると閉じる」といった
   固有の挙動があり、そこは測れていない。
   とくに**削除の確認に使っている `confirm()` が吹き出しの中で
   どう振る舞うかは測れていない。**
   （iframe の中では動くことを実測したため、置き換えはしていない。
   壊れている証拠が無いものを直さない、という判断。）

3. **クラウド同期（`chrome.storage.sync`）の実際の容量超過。**
   超過時の退避処理は書いたが、Google アカウントに繋がる環境が無いため
   実際に上限を超えさせての確認はできていない。

4. **`ssl.gstatic.com` へ到達できる環境での見え方。**
   この作業環境からは出られないため、**常に「学校のフィルタリング下」と
   同じ状態で測っている。** 外部の絵が取れる環境では、これまで通り
   Google の公式アイコンが出るはずだが、実測はしていない。

5. **本物の Google のページ上での差し替え動作。**
   `google.com` へ到達できないため、iframe の検証では応答を差し替えた
   スタブのページを使っている。9つのドットボタンを実際に見つけて
   差し替えられるかは、本物のページでしか確かめられない。

6. **通知（`chrome.notifications`）の実際の表示。**
   API が使えることは確かめたが、通知そのものは出していない。

---

## 人間に確認してほしいこと

1. **`tabs` 権限を残したこと。**
   「すでに開いているタブへ切り替える」機能（迷子防止）に必須で、
   これを外すと機能が壊れる。より狭い代替は無い。
   一方でこの権限は全タブの URL を読めるため、
   学校での配布時に説明が要るかもしれない。

2. **`lib/` へのファイル分割。**
   テストを書くために必要だったが、アーキテクチャの変更にあたる。
   停止条件に従い、**マージの判断は人間に委ねる。**
   実物の Chrome への読み込みで動作は確認済み。

3. **代替アイコンの絵柄。**
   Google の公式ロゴそのものではなく、製品の色に合わせた
   Material 風のグリフを自前で描いた。ロゴを同梱すると商標の扱いが
   面倒になるため。絵柄が学校で通じるかは見てほしい。

4. **`confirm()` を残したこと。**
   吹き出しの中での挙動は測れていないが、壊れている証拠も無い。
   実際に困っている報告があれば、画面内のダイアログに置き換える。

5. **`scripts/lib/project-quality.mjs`（全リポジトリ共通の正本）。**
   このリポジトリからは取得できなかったため入れていない。
   置けば `check-project.mjs` が自動で合成する形にしてある。
